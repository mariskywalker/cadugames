import { StylizedRoom } from '../StylizedRoom'

/**
 * LAYER 2 — chão e tapete do palco (fundo = CSS `.cadu-room-studio-bg`).
 */
export function LayerEnvironment() {
  return (
    <group>
      <StylizedRoom />
    </group>
  )
}
