# 3D scene assets (React Three Fiber)

Optional replacements for the polished cel-shaded room:

| File | Use |
|------|-----|
| `room-backdrop.png` | Referência de cor do estúdio — amostras em `src/constants/roomBackdrop.js` |
| `water-loop.mp4` | Seamless water — also used by `WaterPool.jsx` (copy from `/interactive-map/water.mp4` or add here) |

Cel materials: `src/utils/celShade.js`

## Models (`public/models/`)

| File | Use |
|------|-----|
| `cadu.glb` | Character |
| `tubo de bolhas.glb` | Bubble tube mesh (see `BubbleTubeGlb.jsx`) |
| `balanco.glb` | Sensory swing — circuit station |
| `bolinhaspiscina.glb` | Ball pool — circuit station |
| `barras.glb` | Parallel bars — circuit station |

Circuit layout: `src/constants/therapeuticCircuit.js`
